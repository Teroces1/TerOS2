# 1. Clean the environment variables and target paths
export PREFIX="$HOME/opt/cross"
export TARGET=i686-elf
# export PATH="$PREFIX/bin:$PATH"

# 2. Enter your source folder and see the exact folder names
cd $HOME/src
ls -d */

# Look at the folder output. Ensure the directory names in the next steps match what you see on your screen.
